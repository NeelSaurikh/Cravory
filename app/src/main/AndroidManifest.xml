<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:name=".CravoryApp"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cravory">

        <!-- SplashActivity is now the launcher -->
        <activity
            android:name=".ui.theme.activities.SplashActivity"
            android:exported="true"
            android:label="@string/title_activity_splash"
            android:theme="@style/Theme.Cravory"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity
            android:name=".ui.theme.activities.DishesActivity"
            android:exported="false"
            android:label="@string/title_activity_dishes"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Cravory" />
        <activity
            android:name=".ui.theme.activities.CartActivity"
            android:exported="false"
            android:label="@string/title_activity_dishes"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Cravory" />
        <activity
            android:name=".ui.theme.activities.MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Cravory" />
    </application>

</manifest>
